name: NexusCore API Gateway CI

on:
  push:
    branches: [ main ]
    paths:
      - 'enterprise-platform/api-gateway/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'enterprise-platform/api-gateway/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository source
      uses: actions/checkout@v4

    - name: Set up Go runtime environment
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'
        cache-dependency-path: 'enterprise-platform/api-gateway/go.sum'

    - name: Download Go dependencies
      run: |
        cd enterprise-platform/api-gateway
        go mod download

    - name: Run Go Code Linter
      uses: golangci/golangci-lint-action@v6
      with:
        version: v1.59
        working-directory: enterprise-platform/api-gateway

    - name: Run Gateway Unit and Integration Tests
      run: |
        cd enterprise-platform/api-gateway
        go test -v -race -coverprofile=coverage.out ./...

    - name: Build Secure Container Image
      run: |
        cd enterprise-platform/api-gateway
        docker build -t gcr.io/nexuscore-prod/api-gateway:${{ github.sha }} .
